---
# NoDL interface description for rclpy.lifecycle.LifecycleNode.

nodl_version: 2
description: rclpy.lifecycle.LifecycleNode interface

codegen:
  python:
    role: BASE_CLASS
    module: rclpy.lifecycle
    class: LifecycleNode
    publisher_method: create_lifecycle_publisher

include:
  - ref: local://rclcpp_lifecycle_node.nodl.yaml
