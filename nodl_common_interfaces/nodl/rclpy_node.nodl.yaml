---
# NoDL interface description for rclpy.node.Node.

nodl_version: 2
description: rclpy.node.Node base class interface

codegen:
  python:
    role: BASE_CLASS
    module: rclpy.node
    class: Node

include:
  - ref: local://rclcpp_node.nodl.yaml
