<!--
SPDX-FileCopyrightText: 2026 Open Source Robotics Foundation, Inc.
SPDX-License-Identifier: Apache-2.0
-->

# Schema reference

The NoDL schema is JSON Schema Draft 7.
The reference below is generated from {repo}`nodl_schema/nodl_schema/schemas/nodl.schema.yaml`,
which is the canonical source.

```{jsonschema} ../../nodl_schema/nodl_schema/schemas/nodl.schema.yaml
```

## Parameter schema

Parameter definitions reference
{repo}`nodl_schema/nodl_schema/schemas/parameter.schema.yaml`,
borrowed from `generate_parameter_library`:

```{jsonschema} ../../nodl_schema/nodl_schema/schemas/parameter.schema.yaml
```
